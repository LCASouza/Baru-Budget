# Baru Budget v1.0 — Stable

## Status

IN_PROGRESS — architectural analysis approved on 2026-09-07 (`docs/ARCHITECTURE_ANALYSIS_V1.0.md`); implementation complete, the hardening migration applied to the hosted project and `main` deployed. The ten deferred production checks below remain open, and the version is not DELIVERED until they are closed.

## Objective

Close the first stable version for real family use, with three guarantees: nothing reaches production without passing the tests, the documentation describes what the system actually does, and the security was reviewed end to end instead of assumed.

## Scope

- Continuous integration running lint, build, Vitest and pgTAP on every push and pull request to `main`.
- Security review: the twenty-two `security definer` functions inventoried, their `search_path` and execute grants checked, a scan for versioned secrets, and the client surface confirmed.
- Row level security review: view isolation and function surface covered, closing what the v0.13 matrix does not reach.
- Final suite checked against the list in MASTER_PROMPT section 60.
- Documentation: `README.md` brought up to date, `docs/ARCHITECTURE.md` describing the system as it is, `PROJECT_STATUS.md` reflecting v1.0.
- Backup and import/export validated against the hosted project with real data.
- The desktop and mobile review that v0.13 left pending.
- Version `1.0.0` and a release note.

## Delivered

- `.github/workflows/ci.yml`: lint, build, Vitest and pgTAP on every push and pull request to `main`, with a trimmed Supabase stack for the database step. It publishes nothing; Cloudflare Pages still builds from `main` on its own.
- `docs/SECURITY_REVIEW_V1.0.md`: the twenty-two `security definer` functions inventoried with their `search_path` and grants, the twenty-five invoker functions, the views, the audit rule, the secret scan and the non-transitivity of grants.
- Migration `20260907230100_function_grants.sql`: the one finding of the review, fixed.
- `supabase/tests/security_surface.test.sql`: every function pins `search_path`, none is executable by `anon`, no trigger function is executable by an application role, the bootstrap functions are out of reach, audit cannot be forged on insert or update, and every table with audit columns has both triggers.
- `supabase/tests/view_isolation.test.sql`: every view is covered, two users with the same shape of data never see each other, the third party on the other side of a split sees exactly her side, and `anon` sees nothing.
- `docs/ARCHITECTURE.md`: the system as it is, from the two rules that shape the data model to the environments.
- `README.md` brought up to date, with sections on continuous integration and security.
- Version `1.0.0` in `package.json` and `environment.appVersion`.
- Vitest: 64 files, 392 tests. pgTAP: 34 files, 973 assertions.
- Production serves the build of this version and answers 200.

## Technical Behavior

The analysis recorded the measured starting point: no continuous integration exists, so a push with a broken test publishes anyway; the README still claims v0.10; twenty-two of forty-seven functions in `public` are `security definer`; no private key is versioned and the Angular code never mentions `service_role`; and ten versions carry a deferred production check.

- **The gate exists now.** A push to `main` runs the four commands. Protecting the branch with that check is a setting on GitHub and remains an administrator action.
- **The review became tests.** Three transversal files hold for anything added later: `rls_matrix` over every table, `security_surface` over every function, `view_isolation` over every view. A table, function or view added without its policy, grant or isolation fails the suite instead of waiting for the next review.
- **The finding was hardening, not a hole.** Six trigger functions kept the default grant. A direct call already failed with "trigger functions can only be called as triggers", confirmed as `anon`, so nothing was exposed. After the migration those functions are not even in the PostgREST schema cache: the hosted project answers `PGRST202` instead of exposing them.

## Database Changes

None planned. A gap found by the security review becomes its own migration and is recorded as a fixed bug.

## Security Changes

- `20260907230100_function_grants.sql` revokes execute from `public`, `anon` and `authenticated` on the six trigger functions that still carried the default grant.
- `security_surface.test.sql` and `view_isolation.test.sql` make the review's conclusions permanent.
- Verified on the hosted project after the push: a trigger function is no longer callable or even visible (`PGRST202`), `generate_loan_schedule` answers `42501` to `anon`, and the views return empty.
- No vulnerability was found. Every function pins `search_path`, none is callable by `anon`, and no private key is versioned.

## Tests

- pgTAP: `security_surface.test.sql` (9 assertions) and `view_isolation.test.sql` (23 assertions) join the suite. Result: 34 files, 973 assertions, all passing.
- Vitest: unchanged at 64 files, 392 tests, all passing. Coverage was checked against the list in MASTER_PROMPT section 60 and every item has a home: financial rules, dates, invoices, closing and due dates, instalments, interest, allocations, settlements, recurrences, permissions, row level security, Excel, filters and dashboard.
- `npm run lint` and `npm run build` pass; `npm run db:types` produces no diff, because the migration only changes grants.
- Continuous integration ran on the commit that introduced it, which is the proof that the gate works.

## Bugs Found

| ID | Description | Severity | Status |
|---|---|---|---|
| BUG-003 | Six trigger functions kept the default execute grant while the ones written from v0.8 onwards were revoked, leaving surface with no purpose. A direct call already failed, so nothing was exposed. | Hardening | FIXED |

## Bugs Fixed

- BUG-003, by `20260907230100_function_grants.sql`, guarded from here on by `security_surface.test.sql`.

## Known Limitations

- The ten deferred production checks are open. The version is not DELIVERED until they are closed, and only the administrator can close them.
- Protecting `main` with the continuous integration check is a GitHub setting and has not been done.
- Backup and import/export have been validated against the local database with real seeded rows, not yet against the hosted project with family data.
- There is no staging environment, no production monitoring and no browser end-to-end test. All three are deliberate.

## Out of Scope

Everything listed in MASTER_PROMPT section 65 (Open Banking, bank integration, Pix, investments, native apps, multi-currency and the rest), staging environments, production monitoring and telemetry, browser end-to-end tests, third-party data migration.

## Deferred production checks to close in this version

Ten versions were delivered with their production check deferred by the administrator. This is the first end-to-end pass over the complete system, and v1.0 is not marked DELIVERED while any line stays open.

- [ ] v0.4 Households and Sharing — two users, a household and a grant, checking that VIEW reads and cannot write and that MANAGE writes.
- [ ] v0.5 Dashboard — cards and charts matching the transactions of the period, in the personal and the household contexts.
- [ ] v0.6 Credit Cards and Invoices — a purchase landing on the right invoice, and paying the invoice debiting the account without becoming an expense.
- [ ] v0.7 Installments — a purchase in instalments generating the rows, on a card and on an account.
- [ ] v0.8 Recurrences — generating a month twice and confirming nothing is duplicated.
- [ ] v0.9 Allocations and Settlements — splitting an expense, seeing the balance between people and settling it.
- [ ] v0.10 Loans — the instalment against a calculator, the schedule generated, and the money in without inflating the income of the month.
- [ ] v0.11 Financings — the first and last instalment against a calculator, and the acquisition month recording the down payment and not the asset value.
- [ ] v0.12 Excel Format — export, edit, re-import, and re-import untouched expecting no change.
- [ ] v0.13 Mobile UX — screen reader, full keyboard navigation, the twenty screens at 360 px, 768 px and 1280 px, and the contrast of the two chart colours.

## Completion Criteria

- [x] Continuous integration runs lint, build, Vitest and pgTAP on every push to `main`.
- [ ] `main` is protected by the continuous integration check (administrator action on GitHub).
- [x] `docs/SECURITY_REVIEW_V1.0.md` inventories every `security definer` function with its `search_path` and its execute grants.
- [x] `security_surface.test.sql` and `view_isolation.test.sql` pass and cover every function and view.
- [x] No secret is versioned; the publishable key is the only key in the repository.
- [x] `README.md` and `docs/ARCHITECTURE.md` describe the system as it is.
- [x] `npm run test:db`, `npm test`, `npm run lint` and `npm run build` pass.
- [ ] Backup and import/export validated against the hosted project with real data.
- [ ] Every line of the deferred production checks above is closed.
- [ ] `docs/versions/v1.0.md` and `docs/PROJECT_STATUS.md` updated; version marked DELIVERED only after all criteria are met.
